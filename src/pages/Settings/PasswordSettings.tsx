import React from 'react';
import Main from '../../layouts/Main';

const PasswordSettings = () => {
    return (
        <Main paddingTop useHeader title="Pengaturan Password">
            Hay
        </Main>
    );
};

export default PasswordSettings;
